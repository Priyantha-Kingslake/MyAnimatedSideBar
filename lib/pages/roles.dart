import 'package:flutter/material.dart';

class Roles extends StatelessWidget {
  const Roles({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: const Color.fromARGB(255, 161, 90, 117),
      child: const Center(
        child: Text("Roles"),
      ),
    );
  }
}
