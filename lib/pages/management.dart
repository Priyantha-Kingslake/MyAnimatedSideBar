import 'package:flutter/material.dart';

class Management extends StatelessWidget {
  const Management({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: const Color.fromARGB(255, 88, 188, 185),
      child: const Center(
        child: Text("Management"),
      ),
    );
  }
}
